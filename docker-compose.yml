version: '3.8'

services:
  node:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: pi-node
    ports:
      - "8000:8000"
    environment:
      - NODE_HOST=0.0.0.0
      - NODE_PORT=8000
    restart: unless-stopped
