"""Cut-feature code, quarantined out of the shipped system (ROADMAP C2).

Importable so its own tests can run on demand -- `pytest experimental` -- but
imported by nothing under `packages/`, which `tests/test_experimental_is_quarantined.py`
enforces.
"""
